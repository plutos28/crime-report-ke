<nav class="font-sans flex items-center justify-between flex-wrap p-6">
  <div class="flex items-center flex-shrink-0 text-black mr-6">
    <span class="font-semibold text-2xl text-green-800 tracking-tight">Crime Report | KE Admin </span>
  </div>
  <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
    <div class="text-sm lg:flex-grow">
      <a href="#responsive-header" class="block text-xl mt-4 lg:inline-block lg:mt-0 text-green hover:text-green-600 ml-28 mr-28">
        Data Dashboard
      </a>
      <a href="#responsive-header" class="block text-xl mt-4 lg:inline-block lg:mt-0 text-green hover:text-green-600 mr-28">
        Reports
      </a>
    </div>
    <div>
      <a href="#" class="bg-green-600 inline-block text-xl px-8 py-4 mr-8 leading-none border rounded text-white border-black">Generate Report</a>
      <%= link_to 'Log out', destroy_admin_session_path, method: :delete, class: "text-xl hover:text-red-500" %>
    </div>
  </div>
</nav>

<div class="w-3/5">
  <!--
  Heads up! 👋

  This component comes with some `rtl` classes. Please remove them if they are not needed in your project.
  -->

  <div class="overflow-x-auto">
    <table class="min-w-full divide-y-2 divide-gray-200 bg-white text-sm">
  <thead class="ltr:text-left rtl:text-right">
    <tr>
      <th class="whitespace-nowrap px-4 py-2 font-medium text-gray-900">Title</th>
      <th class="whitespace-nowrap px-4 py-2 font-medium text-gray-900">Description</th>
      <!-- Add other headers as needed -->
      <th class="px-4 py-2"></th>
    </tr>
  </thead>

  <tbody class="divide-y divide-gray-200">
    <% @reports.each do |report| %>
      <tr>
        <td class="whitespace-nowrap px-4 py-2 font-medium text-gray-900"><%= report.title %></td>
        <td class="whitespace-nowrap px-4 py-2 text-gray-700"><%= report.description %></td>
        <!-- Add other columns for report attributes as needed -->
        <td class="whitespace-nowrap px-4 py-2">
          <%= link_to 'View', report_path(report), class: 'inline-block rounded bg-indigo-600 px-4 py-2 text-xs font-medium text-white hover:bg-indigo-700' %>
          <%= link_to 'Edit', edit_report_path(report), class: 'inline-block rounded bg-yellow-500 px-4 py-2 text-xs font-medium text-white hover:bg-yellow-600' %>
          <%= link_to 'Delete', report_path(report), method: :delete, data: { confirm: 'Are you sure?' }, class: 'inline-block rounded bg-red-600 px-4 py-2 text-xs font-medium text-white hover:bg-red-700' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>


  </div>
</div>